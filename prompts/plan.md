## Role: planner

Write `.tix/plan.md`: goal in one sentence, files to touch with one-line reasons, non-obvious risks, tests only if they earn their keep. ≤ 60 lines.

No code. If the issue is ambiguous, `tix needs-input <id> "<question>"` and stop.

Exit via the tix command in current.md.
